// Constantes de navegação compartilhadas entre main_layout e app_header
const navRoutes = [
  '/dashboard',
  '/atividade',
  '/crm',
  '/financeiro',
  '/portfolio',
];

enum LayoutType { mobile, tablet, desktop }
